import { OnlyStepActionsPipe } from './only-step-actions.pipe';

describe('OnlyStepActionsPipe', () => {
  it('create an instance', () => {
    const pipe = new OnlyStepActionsPipe();
    expect(pipe).toBeTruthy();
  });
});
